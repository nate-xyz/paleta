dialog_subdir = join_paths(moduledir, 'dialog')

dialog_sources = [
    '__init__.py',
    'dialog_windows.py',
    'add_color_dialog.py',
    'simple_row.py'
]

install_data(dialog_sources, install_dir: dialog_subdir)